{% extends 'admin/index.html.twig' %}

{% block title %}Gallerie{% endblock %}

{% block body %}
    <h1>Photo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Artiste</th>
                <td>{{ gallerie.artiste }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ gallerie.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_gallerie_index') }}">Retour</a>

    <a href="{{ path('admin_gallerie_edit', {'id': gallerie.id}) }}">Modifier</a>

    {{ include('admin/gallerie/_delete_form.html.twig') }}
{% endblock %}
