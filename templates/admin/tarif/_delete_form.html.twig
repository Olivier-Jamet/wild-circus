<form method="post" action="{{ path('admin_tarif_delete', {'id': tarif.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce tarif?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif.id) }}">
    <button class="btn btn-secondary mb-1">Delete</button>
</form>
